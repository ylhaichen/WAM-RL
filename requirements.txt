# Portable default requirements for WAM-RL.
#
# Install the CUDA-specific PyTorch wheel first, then install this file:
#
#   python -m pip install \
#     torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 \
#     --index-url https://download.pytorch.org/whl/cu124
#   python -m pip install -r requirements.txt
#
# RoboTwin simulator packages are intentionally split into
# requirements-robotwin.txt because CuRobo/PyTorch3D need controlled source
# builds.

-r requirements-cu124.txt
